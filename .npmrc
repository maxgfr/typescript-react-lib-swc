# pnpm configuration
enable-pre-post-scripts=true
# Disable telemetry for CI/CD
shell-emulator=true
# Strict peer dependencies to catch conflicts early
strict-peer-dependencies=false
# Use node_modules linker for better compatibility
node-linker=hoisted
